services:
  cms:
    image: imgreg.chemserv.scc.kit.edu/chemotion-web/cms:latest
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - 8100:1337
    volumes:
      - ./database:/opt/app/database